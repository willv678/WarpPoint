# WarpPoint configuration
# Format: watch_dir=/absolute/path -> sd_base_dir=relative/path/on/sd

watch_dir=/path/to/emulator/saves -> sd_base_dir=switch/saves

# Optional: manual target IP and port override (if UDP discovery fails)
# target=192.168.1.42:40000

